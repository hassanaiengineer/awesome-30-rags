# 26 — AI Blog Search (LangGraph)

**Date:** TBD

**Built by Hassan Khan** • RAG Series

Streamlit app: index web pages into Qdrant and query them using an agentic LangGraph flow.

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
```
