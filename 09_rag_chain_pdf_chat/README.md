# 09 — RAG Chain (PDF Chat)

**Date:** TBD

**Built by Hassan Khan** • RAG Series

Streamlit app: upload PDFs → chunk → embed → store (Chroma) → retrieve → answer (Gemini).

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
```
