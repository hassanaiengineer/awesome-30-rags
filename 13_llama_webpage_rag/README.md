# 13 — Chat With a Webpage (Ollama + Chroma)

**Date:** TBD

**Built by Hassan Khan** • RAG Series

Streamlit app: load a webpage → chunk → embed → retrieve → answer using a local Ollama model.

## Run

```bash
pip install -r requirements.txt
streamlit run "llama3.1_local_rag.py"
```
